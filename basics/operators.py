# Operadores en Python
9 + 3

# Operador de concatenación de cadenas de caracteres
hello = 'Hello'
python = 'Pythonista'
hola_python = hello + ' ' + python
print(hola_python)

# Operadores lógicos o booleanos
x = True
y = False
x or y          # Solo se evalua el segundo operando si el primero es falso.

# Si el primer operando se evalua falso, ent devuelve el primero, si no devuelve el segundo
x and y         # Solo se evalúa el segundo operando si el primero es verdadero.
not a           # Si x se evalúa falso, ent devuelve True, si no devuelve Falso.

x = 0
y = 10
x or y
x and y
not x

# Operadores a nivel de bits
x = 2
x = 7
x | y
x ^ y
x & y
x << 1
x >> 1
~x

# Operadores de pertenencia
lista = [1, 3, 5, 7, 0, 9, 11]
4 in lista
3 in lista
4 not in lista

